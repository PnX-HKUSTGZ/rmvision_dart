topic_recorder:
  output_dir: ./src/topic_recorder/docs
  topics: 
    - name: /image_raw
      type: image
      format: mp4
      fps: 60
    - name: /detector/result_img
      type: image
      format: mp4
      fps: 60